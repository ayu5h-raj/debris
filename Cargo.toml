[workspace]
members = ["sweep-db", "sweep-core", "sweep-app"]
resolver = "2"

[workspace.package]
license = "MIT"
repository = "https://github.com/ayu5h-raj/sweep"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
